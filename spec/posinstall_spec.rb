require 'spec_helper'

describe Posinstall do
  it 'has a version number' do
    expect(Posinstall::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
